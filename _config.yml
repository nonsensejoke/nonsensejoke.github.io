# 基本站点信息
title: My Blog
description: 一个使用 GitHub Pages + Jekyll 的博客

# GitHub Pages 默认使用 kramdown
markdown: kramdown

# 时间设置
timezone: Asia/Shanghai

# 配置 MathJax 渲染公式（如果需要）
kramdown:
  math_engine: mathjax

# 主题（如果你不自定义布局的话）
theme: jekyll-theme-minimal


